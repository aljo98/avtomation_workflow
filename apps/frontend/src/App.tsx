import React from 'react'

export default function App() {
  return (
    <div style={{fontFamily: 'Inter, sans-serif', padding: 24}}>
      <h1>Avtomation Workflow</h1>
      <p>Frontend starter (Vite + React + TypeScript)</p>
      <p>Open <code>/apps/frontend</code> and run <code>npm install</code> then <code>npm run dev</code>.</p>
    </div>
  )
}
